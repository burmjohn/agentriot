Task 3 framework-band upgrade error log
Date: 2026-04-08

Initial failure after upgrading framework-band packages:
- Command: `pnpm typecheck && pnpm lint && pnpm build`
- Failing step: `pnpm lint`
- ESLint version: 10.2.0
- Error:

  TypeError: Error while loading rule 'react/display-name':
  contextOrFilename.getFilename is not a function

Root cause investigation:
- `eslint-config-next@16.2.2` sets `settings.react.version` to `detect`.
- `eslint-plugin-react@7.37.5` still calls `context.getFilename()` during
  React version auto-detection.
- ESLint 10 removes deprecated context members including `context.getFilename`
  in favor of `context.filename`.

Resolution:
- Added a local flat-config override in `eslint.config.mjs`:
  `settings.react.version = "19.2.4"`
- Re-ran `pnpm lint`: exit 0
- Re-ran full verification (`pnpm typecheck && pnpm lint && pnpm build`):
  exit 0
